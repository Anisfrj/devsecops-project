apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-api
  namespace: devsecops-project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask-api
  template:
    metadata:
      labels:
        app: flask-api
    spec:
      containers:
        - name: flask-api
          image: ghcr.io/anisfrj/flask-api:2bff01aba232a26c60942a0f5af254803e283ab6
          ports:
            - containerPort: 5000
          envFrom:
            - configMapRef:
                name: flask-config
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
