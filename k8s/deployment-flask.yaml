apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-api
  namespace: devsecops-project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask-api
  template:
    metadata:
      labels:
        app: flask-api
    spec:
      containers:
        - name: flask-api
          image: ghcr.io/anisfrj/flask-api:473a8e310182b336abed7387a1d8b90e661ae19d
          ports:
            - containerPort: 5000
          envFrom:
            - configMapRef:
                name: flask-config
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
