apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-api
  namespace: devsecops-project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask-api
  template:
    metadata:
      labels:
        app: flask-api
    spec:
      containers:
        - name: flask-api
          image: ghcr.io/anisfrj/flask-api:dd5dc3ea94b40178063ad900b19d1a7b040c8556
          ports:
            - containerPort: 5000
          envFrom:
            - configMapRef:
                name: flask-config
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
